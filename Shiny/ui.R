#ui.R
library(shiny)

# Define UI for application
shinyUI(fluidPage(
  
  # Application title
  titlePanel("Seattle Limebike Data"),
  
  # Sidebar
  sidebarLayout(
    sidebarPanel(

    ),
    
    #Main panel
    mainPanel(

    )
  )
))
